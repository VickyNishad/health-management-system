import 'package:flutter/material.dart';

class TabSlotConfirmation extends StatelessWidget {
  const TabSlotConfirmation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
