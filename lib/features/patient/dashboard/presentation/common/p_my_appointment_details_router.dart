import 'package:flutter/material.dart';

class PMyAppointmentDetailsRouter extends StatelessWidget {
  const PMyAppointmentDetailsRouter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
